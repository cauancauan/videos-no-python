create database LibrasProjeto;
use LibrasProjeto;

create table pessoa(
codigo int not null auto_increment primary key,
nome varchar(120) not null,
senha varchar(120) not null
)engine = InnoDB;

select*from pessoa;

select*from pessoa where nome = 'cauan' and senha = 'cauan675'
